<h1><%= @membership.guest_id %></h1>
<h2><%= @membership.membersclub_id %></h2>

<%= link_to 'Edit', edit_membership_path(@membership) %>
<%= link_to 'Delete', @membership, method: :delete, data: { confirm: "Are you sure?" } %>
